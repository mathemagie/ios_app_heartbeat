{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "publicStreams": {
      "$shareId": {
        ".read": true,
        ".write": "auth != null"
      }
    }
  }
}


